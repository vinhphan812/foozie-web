const Zalopay = require("./modules/zalopay");

(async () => {
     console.log(Zalopay);
     // console.log(await Zalopay.getBanks());
     // await Zalopay.createOrder(
     //      { _id: "8", details: [], total: 500000 },
     //      "localhost:3000"
     // );
})();
